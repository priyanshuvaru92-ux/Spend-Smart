Make the following improvements to my Spend Smart app. DO NOT break or remove any existing feature. Apply ALL changes carefully in one go:

1. Category Icons — Apply Everywhere
Create a single global category icon and color map and use it consistently across the ENTIRE app — dashboard, expense list, budget cards, charts, split expenses, bill reminders, everywhere:
javascriptconst CATEGORY_CONFIG = {
  "Food":          { icon: "🍽️",  color: "#E74C3C" },
  "Transport":     { icon: "🚗",  color: "#3498DB" },
  "Shopping":      { icon: "🛍️",  color: "#9B59B6" },
  "Bills":         { icon: "⚡",  color: "#F39C12" },
  "Health":        { icon: "💊",  color: "#2ECC71" },
  "Travel":        { icon: "✈️",  color: "#1ABC9C" },
  "Entertainment": { icon: "🎬",  color: "#E67E22" },
  "Education":     { icon: "📚",  color: "#2980B9" },
  "Groceries":     { icon: "🛒",  color: "#27AE60" },
  "Rent":          { icon: "🏠",  color: "#8E44AD" },
  "Savings":       { icon: "💰",  color: "#C5A028" },
  "Fitness":       { icon: "🏋️",  color: "#E74C3C" },
  "Subscriptions": { icon: "📱",  color: "#16A085" },
  "Petrol":        { icon: "⛽",  color: "#D35400" },
  "Other":         { icon: "📦",  color: "#7F8C8D" },
};
Apply this config across:

Every expense list row: emoji in a small colored rounded square on the left
Every budget category card: large emoji at the top of the card
Every chart: use matching color per category (pie, bar, doughnut)
Chart legend: show emoji + category name (e.g. "🍽️ Food")
Chart tooltip: show [emoji] Category — ₹amount (X%) e.g. 🍽️ Food — ₹3,200 (34%)
Bar chart X axis: show category emoji below each bar label
Dashboard top spending section: emoji next to each category name + mini horizontal bar in category color
Bill reminders list: category emoji on each bill card
Split expenses: category emoji on each split card
Add Expense form: category dropdown shows emoji + name (e.g. "🍽️ Food")
Budget section cards: large emoji centered at top of each card


2. Replace the App Tagline
Remove the current tagline completely — especially anything mentioning "college students" or "India students" or any student-focused copy.
Replace with this on the login/landing screen:

Main tagline: "Take control of every rupee. Spend smarter, live better."
Subtext: "Track expenses, set budgets, and grow your savings — all in one place."
Display tagline in clean italic or light font weight below the app name
Make it feel premium and professional — not a student project
Remove every mention of "students" or "college" anywhere in the entire app


3. Monthly Budget Page — Full Redesign
Redesign the Monthly Budget page as a fully dedicated section:
Top Summary Bar — 3 cards side by side:

Total Budget Set (sum of all limits)
Total Spent This Month
Remaining (green if positive, red if negative)

Month Selector:

Left/right arrow switcher at the top: < April 2025 >
Switching month loads that month's budget usage history

Category Cards — 2 column grid:
Each card must have:

Category emoji (large, from CATEGORY_CONFIG above) top center
Category name bold below emoji
Small text: "₹spent / ₹limit"
Animated circular ring chart showing % used

Green (0–60%), Yellow (61–85%), Red (86–100%+)
Percentage number in center of ring


Thin progress bar below ring as secondary indicator
Pencil edit icon top right to change limit inline
Red "Overspent" badge if over budget

Add Budget Button:

Floating "+" button bottom right (separate from chatbot button)
Opens bottom sheet modal with: Category dropdown (with emojis), Monthly Limit input, Icon shown automatically from CATEGORY_CONFIG

Empty State:

If no budgets: centered message "No budgets set yet" + "Set Your First Budget" button

Style:

Maroon and gold theme
Cards with subtle border, rounded corners, hover lift effect
Smooth ring fill animation on page load
2 columns desktop, 1 column mobile


4. Settings Page — Clean Redesign
Redesign Settings as a clean organized page with labeled groups and dividers:
Group 1 — Profile:

Avatar circle with user initials or uploaded photo
Click to upload photo (save to Supabase Storage)
Editable: Full Name, Email (read-only), Phone (optional)
Save Changes button → updates Supabase

Group 2 — Currency and Region:

Currency selector with flag + name + symbol:

🇮🇳 Indian Rupee ₹, 🇺🇸 US Dollar ,🇪🇺Euro€,🇬🇧BritishPound£,🇦🇪UAEDirhamد.إ,🇯🇵JapaneseYen¥,🇸🇬SingaporeDollarS, 🇪🇺 Euro €, 🇬🇧 British Pound £, 🇦🇪 UAE Dirham د.إ, 🇯🇵 Japanese Yen ¥, 🇸🇬 Singapore Dollar S
,🇪🇺Euro€,🇬🇧BritishPound£,🇦🇪UAEDirhamد.إ,🇯🇵JapaneseYen¥,🇸🇬SingaporeDollarS


Toggle: "Auto-convert using live rates" (frankfurter.app API)
Currency change updates all amounts across the app instantly

Group 3 — Appearance:

Dark mode toggle (sun/moon icon, saves to localStorage)
Theme color swatches: Maroon (default), Midnight Blue, Forest Green

Group 4 — Notifications:

Toggle: Budget alert notifications
Toggle: Bill reminder notifications
Toggle: Weekly spending summary

Group 5 — Data and Privacy:

Export my data → downloads CSV
Clear all data → confirmation modal before deleting
App version at bottom (v1.0.0)

Group 6 — Account:

Logout button (red, full width, bottom)

Style:

Small grey section label above each group
Clean rows: label left, control right
Lots of whitespace between groups
Maroon and gold theme, fully responsive


5. Floating AI Chatbot Button — Breathing Float Animation
Make the chatbot button a true floating button with gentle movement:
css@keyframes floatUpDown {
  0%   { transform: translateY(0px); }
  50%  { transform: translateY(-8px); }
  100% { transform: translateY(0px); }
}

@keyframes ping {
  0%   { transform: scale(1); opacity: 0.6; }
  100% { transform: scale(1.8); opacity: 0; }
}

.chatbot-float-btn {
  position: fixed;
  bottom: 28px;
  right: 24px;
  z-index: 999;
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background: #800020;
  animation: floatUpDown 3s ease-in-out infinite;
  transition: transform 0.2s, box-shadow 0.2s;
}

.chatbot-float-btn:hover {
  transform: translateY(-8px) scale(1.08);
  box-shadow: 0 8px 24px rgba(197, 160, 40, 0.5);
}

.chatbot-ping {
  position: absolute;
  inset: 0;
  border-radius: 50%;
  background: rgba(197, 160, 40, 0.4);
  animation: ping 2s ease-out infinite;
  pointer-events: none;
}

On mobile (under 640px): bottom: 80px so it clears the bottom nav bar
When chat is open: stop float + ping animations, button becomes X icon smoothly
Chat panel slides up from bottom right as a card — not full screen
Float animation pauses when user is typing in chat


6. Smooth Animations — Apply Throughout Entire App
css@keyframes fadeSlideUp {
  from { opacity: 0; transform: translateY(20px); }
  to   { opacity: 1; transform: translateY(0); }
}

@keyframes slideUp {
  from { transform: translateY(100%); opacity: 0; }
  to   { transform: translateY(0); opacity: 1; }
}

@keyframes modalIn {
  from { transform: scale(0.95); opacity: 0; }
  to   { transform: scale(1); opacity: 1; }
}

@keyframes pageFadeIn {
  from { opacity: 0; transform: translateX(12px); }
  to   { opacity: 1; transform: translateX(0); }
}

@keyframes newItemFlash {
  0%   { border-color: #2ECC71; transform: translateY(-10px); opacity: 0; }
  30%  { border-color: #2ECC71; transform: translateY(0); opacity: 1; }
  100% { border-color: transparent; }
}

.animate-in {
  animation: fadeSlideUp 0.4s ease forwards;
}

.card {
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}
.card:hover {
  transform: translateY(-3px);
  box-shadow: 0 8px 24px rgba(128, 0, 32, 0.15);
}

.btn:active {
  transform: scale(0.96);
  opacity: 0.85;
}

.page-enter {
  animation: pageFadeIn 0.3s ease forwards;
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
Apply staggered entrance to all cards and list items:

1st item: delay 0ms, 2nd: 80ms, 3rd: 160ms, and so on

Number count-up on dashboard:
javascriptfunction countUp(element, target, duration = 1200) {
  let start = 0;
  const step = target / (duration / 16);
  const timer = setInterval(() => {
    start += step;
    if (start >= target) { start = target; clearInterval(timer); }
    element.textContent = Math.floor(start).toLocaleString('en-IN');
  }, 16);
}
Chart animations:

Chart.js: animation: { duration: 800, easing: 'easeInOutQuart' }
Recharts: isAnimationActive={true} with animationDuration={800}

All toggles: smooth 0.25s sliding transition
Active nav item: sliding maroon underline or left border animates into place (0.2s ease)
Bottom sheets: slideUp animation on open
Modals: modalIn scale animation on open
New expense added: newItemFlash animation — slides in from top with green border flash
Page switching: pageFadeIn on every route/section change

⚠️ Final Rules:

Apply ALL 6 changes together cleanly
Do NOT remove any existing feature or Supabase connection
Do NOT change the maroon and gold color theme
Keep all existing pages working — only enhance them
Ensure charts still render correctly after adding emojis to legends
Animations must NOT cause layout shifts or slow the app
All animations respect prefers-reduced-motion
Fully responsive — mobile and desktop
Test everything before finishing


