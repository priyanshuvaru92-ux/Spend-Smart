Replit AI Prompt — Add Receipt Scanner using Gemini 2.5 Flash API
In my existing Spend Smart app, add a Receipt Scanner feature using Gemini 2.5 Flash API. DO NOT change or break any existing code. Only add this new feature.

📷 RECEIPT SCANNER FEATURE:
Where to add it:

In the existing "Add Expense" form, add an "Upload Receipt" button below the form fields
Also add a small 🧾 icon button for quick access

How it should work:

User clicks "Upload Receipt" → file picker opens (accepts jpg, jpeg, png, webp, pdf)
After image is selected, show a preview thumbnail of the receipt
Send the image to Gemini 2.5 Flash API to read and extract receipt details
Auto-fill: Amount, Date, Merchant/Store Name, Category
Show a "🔍 Scanning receipt..." loader while Gemini reads it
If Gemini cannot read something, leave that field empty for user to fill manually


Gemini API call to use:
javascriptconst toBase64 = (file) => new Promise((resolve, reject) => {
  const reader = new FileReader();
  reader.onload = () => resolve(reader.result.split(",")[1]);
  reader.onerror = reject;
  reader.readAsDataURL(file);
});

async function scanReceiptWithGemini(imageFile) {
  const base64Data = await toBase64(imageFile);
  const mimeType = imageFile.type; // e.g. "image/jpeg"

  const response = await fetch(
    `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-04-17:generateContent?key=YOUR_GEMINI_API_KEY`,
    {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        contents: [{
          parts: [
            {
              inline_data: {
                mime_type: mimeType,
                data: base64Data
              }
            },
            {
              text: `This is a receipt image. Extract details and reply ONLY in pure JSON with no markdown, no backticks, no extra text:
{
  "amount": number or null,
  "date": "YYYY-MM-DD" or null,
  "merchant": "store name" or null,
  "category": "Food/Travel/Shopping/Bills/Health/Other"
}`
            }
          ]
        }],
        generationConfig: {
          temperature: 0.1
        }
      })
    }
  );

  const data = await response.json();
  const rawText = data.candidates[0].content.parts[0].text;
  const cleaned = rawText.replace(/```json|```/g, "").trim();
  const extracted = JSON.parse(cleaned);
  return extracted;
}

Then use it in the Upload button like this:
javascriptconst handleReceiptUpload = async (e) => {
  const file = e.target.files[0];
  if (!file) return;
  setScanning(true);
  try {
    const result = await scanReceiptWithGemini(file);
    if (result.amount) setAmount(result.amount);
    if (result.date) setDate(result.date);
    if (result.merchant) setMerchant(result.merchant);
    if (result.category) setCategory(result.category);
    setSuccessMsg("✅ Receipt scanned! Please verify the details.");
  } catch (err) {
    setErrorMsg("⚠️ Could not read receipt. Please fill manually.");
  }
  setScanning(false);
};

UI Details:

"🧾 Upload Receipt" button matches existing maroon/gold color theme
Show receipt image preview after upload
While scanning: "🔍 Scanning receipt with Gemini AI..."
On success: "✅ Receipt scanned! Please verify the details." in green
On failure: "⚠️ Could not read receipt. Please fill manually." in yellow
Save the receipt image preview alongside the expense record